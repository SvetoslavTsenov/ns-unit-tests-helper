<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
    <Page.actionBar>
        <ActionBar title="My" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <StackLayout class="p-20" >
        <Button text="take screenshot" tap="{{ onTap }}" /> 
        <Button text="compare image" tap="{{ sendImageData }}" />
        <Image src="{{ img }}" stretch="none" />
    </StackLayout>
</Page>
